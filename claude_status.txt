项目：小懂哥 v2.3 发布
日期：2026-08-15
状态：v2.3 已推送并发布 GitHub Release（tag v2.3），README 已同步

## v2.3 发布（已完成）

1. 备份导出/导入闭环修复——导出过滤设备本地 backup-health 键，导入白名单兼容旧备份；撤销快照原生端改存 SQLite 单行表 UndoBackup（绕开 WebView localStorage 5MB 配额）。
2. Web 读取容错对齐原生端——单条损坏跳过并告警，不阻塞整列表；备份导入仍走严格校验。
3. 死代码清理 + shareCard 去重（DEFAULT_PRIVACY/downloadBlob 单一来源）。
4. 测试护栏——shared 语义分析、日期/天气、备份解析链三套 node:test 单元测试（23 用例，零新依赖），`npm test` 可运行。
5. 版本 2.2 → 2.3，versionCode 13 → 14。

正式签名 APK：release/xiaodongge-v2.3.apk（SHA-256 c489a58a…8123，签名证书与 v2.2 一致，可覆盖安装）。APK 不进入 Git。

## 本轮提交（本地 main 领先 origin/main 7 个）

- 37c0a16 docs: record 2026-08-07 cleanup in claude_status.txt
- ac953d4 test: add node:test suites for shared analysis and date rules
- f74532c fix: restore backup import/export loop and align storage tolerance
- f05a52b chore: remove dead exports and dedupe download helpers
- f5e4c11 docs: review metadata fallback chain with gap list
- （release: v2.3 (versionCode 14)）
- （docs: update README to v2.3）

## 后续注意

- 代码检查用 npm run typecheck + npm run mobile:build + npm test 即可。
- 构建 APK：npm.cmd run android:build:release（正式，需四项环境变量）。
- 评审文档 docs/superpowers/specs/2026-08-15-metadata-fallback-chain-review.md 记录 4 个未做缺口：G1 截图 OCR 目录补全（高）、G2 暂停状态媒体读取（中）、G4 OCR 空识别感知（低）；G3/G5 已推迟。
- Codex 交接待办（未完成）：Android 真机交互验收——保存位置、取消保存、分享附件 MIME、复制粘贴、重复点击保护（见 docs/session_summary_2026-08-04_codex.md）。
