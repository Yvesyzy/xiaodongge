# 小懂哥 v2.6.2

首页恢复黑胶、纸张纹理与墨绿/暖棕入口，标题和草稿放在独立浅色区域，最近三条记录仍然前置。保留十分制、记录日期和年度分页导出。

- 修复 Android JSON 导入的 `ExecuteSet: No value for values`，以及含复听记录时的外键写入顺序。
- 兼容旧月份归档方式的月报快照，修复选取 JSON 文件后的异步报错。
- 支持无封面 JSON 导出；导入时可跳过备份封面并保留当前封面，乐评和总结正常恢复。
- 保留导入前快照、事务回滚和撤销，切换封面选项时更新预览及确认说明。

版本为 **2.6.2 (18)**，包名 `com.yves.musicarchive`。沿用 v2.5/v2.6/v2.6.1 正式签名，可直接覆盖安装，无需卸载。更早旧签名或 debug 版本应先备份再迁移。

APK SHA-256：`632cd41e5f69d930108ac50ce77236b8246c6af7dba805d84aca414824449097`。

验证通过：类型检查、31 项单元测试、首页 0/6/128 条记录与 320—1280px 布局/跳转、旧月报与文件选择/封面选项、桌面真实 SQLite 导入导出/回滚/撤销、Android assembleRelease/lintVitalRelease、正式签名与版本及 APK Web 资源哈希。桌面 SQLite 检查模拟 Android executeSet 参数约束，手机安装与系统交互仍需真机确认。
